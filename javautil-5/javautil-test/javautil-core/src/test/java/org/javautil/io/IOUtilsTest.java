package org.javautil.io;

import static org.junit.Assert.assertEquals;

import java.io.IOException;

import org.junit.Test;

public class IOUtilsTest {
	/*
	 * static final String text = "Ascii text file\n";
	 * 
	 * @Skip
	 * 
	 * @Test public void getResourceAsStringTest() throws IOException { String
	 * ret = IOUtils.getResourceAsString("org/javautil/io/readme.text");
	 * assertEquals(ret,text); }
	 * 
	 * @Skip
	 * 
	 * @Test public void getResourceAsBytesTest() throws IOException { byte[]
	 * ret = IOUtils.getResourceAsBytes("org/javautil/io/readme.text"); byte[]
	 * expected = text.getBytes(); for (int i = 0; i < ret.length; i++) {
	 * assertEquals(ret[i],expected[i]); }
	 * 
	 * }
	 */
}
