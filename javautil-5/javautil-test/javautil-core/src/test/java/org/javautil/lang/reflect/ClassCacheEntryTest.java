package org.javautil.lang.reflect;

import org.apache.log4j.Logger;
import org.junit.Test;

public class ClassCacheEntryTest {
	Logger logger = Logger.getLogger(getClass());

	// TODO fix or remove
	@Test
	public void test1() {
		// TestBean bean = new TestBean();
		// ClassCacheEntry classEntry = new ClassCacheEntry(bean.getClass());
		// classEntry.populate();
		// FieldCache fields = classEntry.getFields();
		// assertEquals(42, fields.size());
		// FieldCacheEntry field = fields.get("rawAddr1");
		// assertNotNull(field);
		// Method getterMethod = field.getGetterMethod();
		// assertNotNull(getterMethod);
		// assertEquals("getRawAddr1", field.getGetterMethod().getName());
	}
}
