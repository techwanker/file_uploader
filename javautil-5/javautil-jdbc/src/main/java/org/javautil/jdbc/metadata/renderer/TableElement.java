package org.javautil.jdbc.metadata.renderer;

/**
 * @todo convert to dom4j
 *       <p>
 *       Title:
 *       </p>
 * 
 *       <p>
 *       Description:
 *       </p>
 * 
 *       <p>
 *       Copyright: Copyright (c) 2005
 *       </p>
 * 
 *       <p>
 *       Company:
 *       </p>
 * 
 * @author not attributable
 * @version 1.0
 */
// @todo throw this out replace with jaxb
// table definitions are automatically created by oracle now
public class TableElement {

	// public static Element asElement(final Table t, final boolean
	// emitCamelCase) {
	// final Element el = DocumentHelper.createElement(XmlMeta.ENTITY);
	// el.addAttribute(XmlMeta.ENTITY_NAME , t.getTableNameAsAttributeName());
	// el.addAttribute(XmlMeta.TABLE_NAME,t.getTableName());
	// addAttributes(el,t, emitCamelCase);
	// addPrimaryKeyColumns(el,t);
	// addReferences(el,t);
	// addReferencedBy(el,t);
	// return el;
	// //
	// }

}
